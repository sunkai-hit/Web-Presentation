# Shared Resources

本目录只存放真正跨项目复用的资源，不存放任何单一项目专属内容。

- `components/`：可复用页面组件或组件规范
- `styles/`：公共样式、布局和设计令牌
- `assets/`：通用图标、纹理、背景等非项目专属资源
- `scripts/`：跨项目共享的前端辅助脚本

如果某项内容只服务于一个项目，应放回 `projects/<slug>/` 内。
